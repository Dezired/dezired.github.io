Drop your project images and videos in this folder, then reference
them from the `PROJECTS` array in `js/main.js` (e.g. `assets/projects/robotic-arm.jpg`).

See the main README's "Add real projects" section for size/format tips.
